# gds-entra-infrastructure-modules

Repo where we store reusable terraform modules for the gds-entra solution.

## Prerequisites

- Install pre-commit
 `pre-commit install`


## Github workflows

All workflow have been disabled in this repo until we can reach a decision about how we can best use the actions within.
