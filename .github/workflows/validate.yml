name: Validate

on:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main

permissions:
  contents: read
  pull-requests: write

jobs:
  pre-commit-checks:
    uses: alphagov/gds-github-workflows/.github/workflows/terraform-pre-commit-checks.yml@29b2abf32227088428af98e8a9278d62e6dd0c93 # v0.0.7
    permissions:
      contents: read
    with:
      terraform-version: ${{ vars.TERRAFORM_VERSION || null }} # defined by reusable workflow if not set
